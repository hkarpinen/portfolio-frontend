{
  "extends": ["next/core-web-vitals", "plugin:@tanstack/eslint-plugin-query/recommended"],
  "plugins": ["unused-imports", "@typescript-eslint"],
  "rules": {
    "react/jsx-no-comment-textnodes": "off",
    "@typescript-eslint/no-unused-vars": "error",
    "@typescript-eslint/consistent-type-imports": "error",
    "unused-imports/no-unused-imports": "error",
    "unused-imports/no-unused-vars": [
      "warn",
      {
        "vars": "all",
        "varsIgnorePattern": "^_",
        "args": "after-used",
        "argsIgnorePattern": "^_"
      }
    ],
    "react/self-closing-comp": "error",
    "react/jsx-boolean-value": ["error", "never"],
    "react/jsx-curly-brace-presence": ["error", { "props": "never", "children": "never" }],
    "import/no-duplicates": "error",
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["@/components/editorial/*"],
            "message": "Import editorial components from the barrel: `import { … } from \"@/components/editorial\"`. Deep paths are reserved for sibling imports inside components/editorial itself."
          }
        ]
      }
    ],
    "no-restricted-syntax": [
      "error",
      {
        "selector": "CallExpression[typeArguments][callee.object.name='api'][callee.property.name=/^(get|post|put|patch|delete|upload)$/]",
        "message": "Blind generic on api.<verb><T> bypasses zod validation. Use api.parsed.<verb>(path, schema) for typed responses, or api.send.<verb>(path, body?) for fire-and-forget."
      },
      {
        "selector": "CallExpression[typeArguments][callee.name='serverFetch']",
        "message": "Blind generic on serverFetch<T> bypasses zod validation. Use parsedServerFetch(path, schema, cookieHeader)."
      }
    ]
  },
  "overrides": [
    {
      "files": ["components/editorial/**/*.{ts,tsx}"],
      "rules": {
        "no-restricted-imports": "off"
      }
    }
  ]
}
